iname: Update dependencies

on:
  workflow_dispatch:
  schedule:
    # Runs at 00:00 JST every day
    - cron: '0 15 * * *'

jobs:

  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Deno
        uses: denoland/setup-deno@v1

      - name: Update dmm
        run: |
          deno install \
            --allow-read=deps.ts,dev_deps.ts \
            --allow-write=deps.ts,dev_deps.ts \
            --allow-net='cdn.deno.land,api.deno.land,x.nest.land,raw.githubusercontent.com,github.com,api.github.com' \
            https://deno.land/x/dmm/mod.ts
          cd src
          dmm update
          dmm update --deps-file dev_deps.ts

      - name: Create pull request
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: 'chore(deps): 依存関係を更新'
          title: 'chore: 依存関係を更新'
          body: 'This pull request was auto-generated by GitHub Actions.'
          branch: update-dependencies
